# Claude Code Handoff - MSG Закупки Calculator

## Текущая версия: v4.61

## Основной файл для работы
```
c:\Users\Skylake\msgnn-calculator\index.html
```

## Референсный калькулятор для сравнения
```
https://nn.23met.ru/calculator
```

## База данных металлов (только чтение, не менять)
```
c:\Users\Skylake\metal-calculator\docs\database\metals.json
```

## Библиотека калькулятора (только чтение, не менять)
```
c:\Users\Skylake\metal-calculator\src\calculator.js
```

---

## ЧТО УЖЕ СДЕЛАНО (v4.30 → v4.61)

### Исправленные типы металла:
- ✅ Круг, Круг оц. - добавлены марки стали (45 штук) + коэффициент оцинковки 1.03
- ✅ Лента/штрипс - добавлены в SHEET_TYPES для ширины/длины
- ✅ Лист рифленый - исправлен ключ sheet_riffle → sheet_checkered
- ✅ Листы (г/к, х/к, оцинк, окраш) - работают корректно
- ✅ Полоса, Полоса оцинк. - исправлен parseSize для strip_linear
- ✅ Проволока, Проволока оцинк. - убраны из TYPES_NEEDING_STEEL_COEFFICIENT (база уже обрабатывает)
- ✅ Трубы б/ш (г/д, х/д, котельная, ВГП) - добавлены формулы в stringFormulasAsIs
- ✅ Сутунка - добавлена в stringFormulasAsIs
- ✅ Рельс - добавлен выбор варианта (ГОСТ, EN, AREMA) - РАБОТАЕТ
- ✅ v4.41: Трубы (квадр. оц., овал., овал. оц., плоскоовал., ПНД) - добавлены формулы
- ✅ v4.42: Бесшовные трубы - добавлены STANDARD_LENGTHS
- ✅ v4.43: Швеллер гнутый, Швеллер гнутый оц. - добавлены в stringFormulasAsIs
- ✅ v4.43: Шпунты (Ларсена, двутавровый, ПВХ, трубчатый, холодногнутый) - добавлен shpynt_3fields
- ✅ v4.44: Шпунт двутавровый - убрана нормализация / → \ (в базе коэффициенты с /)
- ✅ v4.45: Арматура А1/А3 - добавлены табличные ГОСТ коэффициенты (ARMATURE_GOST_COEFFICIENTS)
- ✅ v4.46: Арматура А1 оц./А3 оц. - добавлен коэффициент оцинковки 1.03
- ✅ v4.47: Листы - разблокировано поле "Метры" (показывает площадь м²)
- ✅ v4.48: Переименовано поле "Длина, м" → "Метры"
- ✅ v4.49-v4.50: Лист ПВ - добавлен выбор ТУ стандарта (sizeStandards)
- ✅ v4.51: Лист рифленый - добавлен выбор типа рифления (riffleTypes)
- ✅ v4.52: Лист/рулон окраш. - добавлен выбор типа оцинковки (zincOptions)
- ✅ v4.53: Переименовано поле "Сталь/Стандарт" → "Сталь/Стандарт/Оцинк."
- ✅ v4.54: Лист/рулон оцинк. - добавлен выбор типа оцинковки (zincOptions)
- ✅ v4.55: При выборе н/д (немерная длина) появляется поле для ручного ввода
- ✅ v4.56: Авторизация сохраняется в localStorage (не сбрасывается при перезагрузке)
- ✅ v4.57: Добавлено поле "Комментарий менеджера" перед отправкой поставщикам
- ✅ v4.58: request_id теперь на основе timestamp (без конфликтов между устройствами)
- ✅ v4.59: Улучшена совместимость скачивания Excel с Яндекс Браузером
- ✅ v4.60: Округление штук теперь ВВЕРХ (Math.ceil вместо Math.round)
- ✅ v4.61: Обновлена версия системы

### Исправленные общие проблемы:
- ✅ При смене размера теперь пересчитывается вес (onSizeChange → recalculateRow)
- ✅ При смене марки стали пересчитывается вес
- ✅ Добавлен выбор стандарта для профнастила и рельсов (TYPES_WITH_STANDARDS)
- ✅ v4.38: Исправлен Профнастил окраш./оц. - добавлена поддержка формата result.actual.weight

---

## КЛЮЧЕВЫЕ МЕСТА В КОДЕ (index.html)

### Константы типов металла (~строки 900-930):
```javascript
const TYPES_WITHOUT_PIECES = [...];  // Без поля "штуки"
const FASTENER_TYPES = [...];        // Крепёж
const SHEET_TYPES = [...];           // Листовые типы
const TYPES_WITH_STANDARDS = ['profnastil_okrash', 'profnastil_ocink', 'rels', 'sheet_pv', 'sheet_pv_galv', 'sheet_checkered', 'sheet_painted'];
```

### Функция получения стандартов (~строка 1167):
```javascript
function getStandardsForMetal(metalKey, size) { ... }
```

### Рендер строки таблицы (~строка 1600):
```javascript
function renderRow(item, index) { ... }
```

### Обработчики изменений (~строка 1790):
```javascript
function onSizeChange(index, value) { ... }
function onFieldChange(index, field, value) { ... }
```

### Пересчёт строки (~строка 1980):
```javascript
function recalculateRow(index, changedField) { ... }
```

### Передача стандарта в calculateMetal (4 места):
Искать: `params.standard = standardObj`
- ~строка 1895
- ~строка 2005
- ~строка 2295
- ~строка 2835

---

## КАК ОТЛАЖИВАТЬ

1. Открыть Chrome DevTools (F12)
2. Вкладка Console - смотреть ошибки
3. В коде много console.log с эмодзи:
   - 📋 - парсинг
   - 🔄 - пересчёт
   - 📊 - результат
   - ❌ - ошибка
   - ✅ - успех

---

## КОММИТЫ И ДЕПЛОЙ

```bash
cd c:\Users\Skylake\msgnn-calculator
git add index.html
git commit -m "fix: описание - vX.XX"
git push
```

Сайт автоматически обновляется на:
https://lamblackout.github.io/msgnn-calculator/

---

## СТРУКТУРА ДАННЫХ

### Профнастил в базе:
```json
{
  "profnastil_okrash": {
    "formula": "profnastil_area",
    "sizes": ["НС35", "Н60", ...],
    "standards": {
      "НС35": [
        { "name": "0.6х1000 ГОСТ 24045", "coefficient": 6.4 },
        { "name": "0.7х1000 ГОСТ 24045", "coefficient": 7.4 }
      ]
    }
  }
}
```

### Рельсы в базе:
```json
{
  "rels": {
    "formula": "rels_linear",
    "railTypes": ["КР70", "Р65", ...],
    "variants": {
      "КР70": [
        { "name": "ГОСТ Р 53866-2010", "coefficient": 6.047 }
      ]
    }
  }
}
```

### Формула профнастила (calculator.js ~строка 1140):
- Ожидает params.standard как объект {name, coefficient}
- Или как JSON строку которую можно распарсить

---

## ТЕКУЩИЕ ПРОБЛЕМЫ И НЕРЕШЕННЫЕ ВОПРОСЫ

### ✅ Проблема с отображением названий поставщиков в Excel сводке (2026-01-28)

**Статус:** РЕШЕНО - показываем полный email адрес

**Описание проблемы:**
В Excel сводке цен от поставщиков ([Get Supplier Summary v2.json](n8n-workflows/Get%20Supplier%20Summary%20v2.json)) названия колонок выглядят непонятно. Например: "smp1 (цена)" вместо понятного названия компании или email адреса.

**Техническая причина:**
1. При получении email от поставщика ([IMAP Trigger - Save Incoming Emails.json:26-48](n8n-workflows/IMAP%20Trigger%20-%20Save%20Incoming%20Emails.json#L26-L48)) система извлекает `supplier_name`:
   - Если в email есть display name (типа "Михаил Иванов <mikhail@company.ru>") → берётся "Михаил Иванов"
   - Если нет display name (просто "smp1@metalsnab.ru") → берётся только часть до @ → "smp1"

2. При формировании сводки ([Get Supplier Summary v2.json:214](n8n-workflows/Get%20Supplier%20Summary%20v2.json#L214)):
   ```javascript
   const colName = supplier.name.substring(0, 20);
   row[`${colName} (цена)`] = price || '—';
   ```
   Результат: колонка "smp1 (цена)" - непонятно, кто это.

**Отправленное сообщение заказчику (2026-01-28):**
```
Михаил, разобрались с таблицей.

Система работает так: когда поставщик отвечает на письмо, мы автоматически забираем название из его email. Если у него в настройках почты указано имя (например, "ООО МеталлСнаб"), то показываем его. Если имени нет, берём часть email адреса до символа @ (например, из smp1@company.ru получается просто smp1).

Проблема в том, что не все поставщики настраивают свои почтовые программы - у кого-то стоит нормальное название компании, у кого-то вообще ничего не указано. Из-за этого в таблице получается непонятно.

Предлагаем решение: везде показывать полный email адрес поставщика вместо имени. Тогда будет "smp1@metalsnab.ru (цена)" вместо "smp1 (цена)". Так всегда понятно, кто именно ответил, и можно быстро найти письмо от этого поставщика.

Если у вас есть список известных поставщиков с понятными названиями компаний, можем сделать дополнительно справочник (email → название), но это уже отдельная задача.

Как вам такой вариант с полным email?
```

**Возможные решения:**
1. **Всегда показывать полный email** (smp1@metalsnab.ru) → простое решение, всегда понятно
2. **Создать справочник** email → название компании → требует ручного заполнения известных поставщиков
3. **Гибридный подход** - если есть display name, показываем его, иначе полный email

**Решение (реализовано 2026-01-28):**
Заказчик подтвердил, что полный email адрес будет удобнее. Внесены изменения в `n8n-workflows/Get Supplier Summary v2.json` - узел "Code: Build Summary":

```javascript
// Было:
const colName = supplier.name.substring(0, 20);

// Стало:
const colName = supplier.email;  // Полный email адрес
```

**Результат:**
- Было: "smp1 (цена)"
- Стало: "smp1@metalsnab.ru (цена)"

**Файлы изменены:**
- ✅ `n8n-workflows/Get Supplier Summary v2.json` - узел "Code: Build Summary" (2 места: colName и suppliers list)

**Примечание:** Workflow нужно переимпортировать в n8n на Railway после изменений.
